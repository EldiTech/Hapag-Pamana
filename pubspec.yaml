name: hapag_pamana
description: "HapagPamana Catering Services — customer app for browsing the menu and catering packages, creating an account, and booking events."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Flutter's own translations (Material / Cupertino / Widgets), so the language
  # a member picks in Settings turns over everything the framework supplies —
  # the booking wizards' date and time pickers, dialog buttons, the
  # text-selection menu and date formatting. See lib/main.dart.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Professional typography for the brand (Playfair Display + Montserrat).
  google_fonts: ^8.1.0

  # Firebase — the app reads live content (products, etc.) the Content
  # Moderator publishes to Firestore, and lets customers create accounts.
  firebase_core: ^4.11.0
  cloud_firestore: ^6.6.0
  firebase_auth: ^6.5.4

  # Lets a signed-in member pick a profile photo from their gallery; the image
  # is downscaled and stored inline (data URL) on customers/{uid}.
  image_picker: ^1.1.2

  # Opens external links from the About screen — the dialer, e-mail, Google
  # Maps and the social pages — and hands the member to PayMongo's hosted
  # checkout page for a booking's downpayment. Falls back to a clipboard copy
  # when no app can handle the link.
  url_launcher: ^6.3.1

  # Talks to PayMongo's Checkout Sessions API for the 50% downpayment a booking
  # is held with (lib/data/paymongo_service.dart).
  http: ^1.5.0

  # The address step on the catering wizard: an embedded, draggable-pin map
  # plus Google Places autocomplete (lib/data/places_service.dart,
  # lib/screens/user/address_picker_page.dart).
  google_maps_flutter: ^2.13.0

  # Loads .env at startup so API keys and other secrets live outside source
  # control instead of as hardcoded defaults (see .env.example, lib/env.dart).
  flutter_dotenv: ^5.2.1

  # Renders the Gabay mark (Assets/Gabay.svg) crisply at any size.
  flutter_svg: ^2.2.1

  # Hosts the 3D Layout Viewer (hosting/layout-viewer) inside the app — the
  # same Three.js scene the Layout Designer desk draws with, so a member can
  # orbit their event's floor plan and walk through it. See
  # lib/screens/user/layout_preview_page.dart.
  webview_flutter: ^4.10.0

dependency_overrides:
  objective_c: ^9.5.0
  jni: ^1.0.3
  jni_flutter: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates branded launcher icons (Android adaptive + legacy, iOS) from the logo.
  flutter_launcher_icons: ^0.14.4

# Launcher-icon generation (run: dart run flutter_launcher_icons).
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "Assets/app_icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  background_color_ios: "#F6EFDF"
  adaptive_icon_background: "#F6EFDF"
  # Logo pre-inset to the adaptive safe zone (~66%) so masks never clip the crest.
  adaptive_icon_foreground: "Assets/app_icon_fg.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - Assets/HapagPamana.png
    - Assets/Gabay.svg
    - .env

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
